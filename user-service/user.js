exports.create = function(email, password, username, done) {
  // Create the user and call 'done(null, id)' where id is a string.
}

exports.get = function(id, done) {
  // Finds the user and calls 'done(null, user)'
}
